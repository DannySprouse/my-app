import React from "react";
import "./Title.css";

const Title = props => (
    <div className="titlebox">
        <h1>Cat Breed React Clicky Game!</h1>
        <p>Click on any image.</p>
    </div>
);

export default Title;